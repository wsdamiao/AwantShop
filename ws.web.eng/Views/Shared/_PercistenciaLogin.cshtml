@using ws.eng.obj
@using ws.web.eng.Models
@model ws.web.eng.Models.LoginViewModel


@{

    if ((Paginas)Session["pagAtual"] == Paginas.Login)
    {
        @Html.Hidden("Projeto", (SimuladorDeProjetoViewModel)Model.Projeto)
    }
}